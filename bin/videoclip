#!/bin/bash

USAGE="Usage: videoclip INPUTFILE OUTPUTFILE STARTAT DURATION"

if [ "$#" != 4 ]; then
	echo "$USAGE"
	exit 1
fi

INPUT_FILE=$1
OUTPUT_FILE=$2
START_AT=$3
DURATION=$4

ffmpeg -i $INPUT_FILE -ss $START_AT -t $DURATION -async 1 -strict -2 $OUTPUT_FILE
